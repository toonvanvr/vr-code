export interface ApplicationOptions {
  /** HTML tag for the root element and prefix for custom elements */
  htmlTag: string
}

export interface ApplicationRegisterHTMLElementsOptions {
  /** HTML tag for the root element and prefix for custom elements */
  tag: string
}
